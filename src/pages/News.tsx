import { Box, Container } from "@mui/material";
import React, { FC } from "react";
import Post from "../components/post";

const News: FC = () => {
  return (
    <Container>
      <Box sx={{}}>
        <Post />
      </Box>
    </Container>
  );
};

export default News;
